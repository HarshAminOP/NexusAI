# --- Core Framework ---
langgraph>=0.2.0
langchain-openai>=0.1.0
python-dotenv>=1.0.0

# --- Persistence Layer ---
# SQLite for your local Mac ARM development
langgraph-checkpoint-sqlite>=2.0.0

# DynamoDB for your AWS production deployment
# Note: This is environment-aware and only used if IS_AWS is True
langgraph-checkpoint-aws>=1.0.0

# --- Networking & Utilities ---
# We will need these for the FastAPI bridge in Module 4
fastapi>=0.110.0
uvicorn>=0.29.0
typing-extensions>=4.10.0